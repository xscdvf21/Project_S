using System.Collections;
using System.Collections.Generic;
using UnityEngine;
[CreateAssetMenu(fileName = "PassiveSKill_Data", menuName = "ProjectS/CreateSkill/Passive", order = int.MaxValue)]
public class PassiveSkill_Object : ScriptableObject
{
    public PASSIVE_SKILL type;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
