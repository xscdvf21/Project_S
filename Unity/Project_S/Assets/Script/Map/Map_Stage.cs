using System.Collections;
using System.Collections.Generic;
using UnityEngine;


[CreateAssetMenu(fileName = "Map_Data", menuName = "ProjectS/CreateMap/Map", order = int.MaxValue)]
public class Map_Stage : ScriptableObject
{
    public GameObject map;
    public int subStage;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
